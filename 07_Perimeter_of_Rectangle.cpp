#include <iostream>
using namespace std;

void main()
{
    float length,breadth,perimeter;
    cout<<"Enter length: ";
    cin>>length;
    cout<<"Enter breadth: ";
    cin>>breadth;
    perimeter=2*(length+breadth);
    cout<<"Perimeter of Rectangle = "<<perimeter;
}
